// for loop

var participants = ["maxmillian", "beyonce", "jacob", "nicklas", "saveus"];

for (let i = 0; i < participants.length; i++) {
    let part = participants[i];
    console.log(`hello, ${part}`);
}